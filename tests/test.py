import torch
# a = torch.nn.BCEWithLogitsLoss(**bce_kwargs)
# # v = a.new_zeros(*args, **kwargs)
a.to(*args, **kwargs)
# # x.new_zeros(*shape, requires_grad=True, dtype=torch.float32, pin_memory=False)
